node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local durable paper-runtime state (never commit account state)
data/paper-runtime.json
data/paper-runtime.json.tmp-*
data/paper-runtime.json.corrupt-*

# Database backups contain account state; keep them outside git.
backups/
